# CSCI_576_Proj_1
OpenCV must be installed prior and configured on the CMake before running

To run the code go into the working folder and execute
cmake --build build\

then run

build\Debug\MyImageApplication.exe
